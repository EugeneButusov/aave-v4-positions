{
  "name": "@aave-positions/enrichment",
  "version": "0.0.0",
  "private": true,
  "type": "commonjs",
  "description": "What Aave does not put in an event: what each token calls itself, and what the Spoke's oracle prices its reserves at",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json && node -e \"require('node:fs').cpSync('src/postgres-migrations','dist/postgres-migrations',{recursive:true})\"",
    "build:watch": "tsc -p tsconfig.build.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@aave-dao/aave-address-book": "catalog:",
    "@clickhouse/client": "catalog:",
    "@nestjs/common": "catalog:",
    "@packages/clickhouse": "workspace:*",
    "@packages/indexing": "workspace:*",
    "@packages/postgres": "workspace:*",
    "postgres": "catalog:",
    "reflect-metadata": "catalog:",
    "rxjs": "catalog:",
    "viem": "catalog:"
  },
  "devDependencies": {
    "@aave-positions/events": "workspace:*",
    "@aave-positions/positions": "workspace:*",
    "@nestjs/testing": "catalog:",
    "@packages/migrations": "workspace:*"
  }
}
