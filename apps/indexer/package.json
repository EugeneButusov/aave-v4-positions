{
  "name": "@aave-v4-positions/indexer",
  "version": "0.0.0",
  "private": true,
  "type": "commonjs",
  "description": "Ingests Aave v4 Spoke and Hub events and folds them into position state",
  "main": "dist/main.js",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "start": "node --enable-source-maps dist/main.js",
    "backfill": "node --enable-source-maps dist/backfill.js",
    "start:dev": "pnpm run build && concurrently -k -n tsc,app -c blue,green \"tsc -p tsconfig.build.json --watch --preserveWatchOutput\" \"node --watch --enable-source-maps dist/main.js\"",
    "start:debug": "pnpm run build && concurrently -k -n tsc,app -c blue,green \"tsc -p tsconfig.build.json --watch --preserveWatchOutput\" \"node --watch --inspect --enable-source-maps dist/main.js\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:cov": "vitest run --coverage",
    "migrate": "node dist/migrate.js"
  },
  "dependencies": {
    "@aave-positions/events": "workspace:*",
    "@packages/clickhouse": "workspace:*",
    "@packages/indexing": "workspace:*",
    "@packages/ops": "workspace:*",
    "@clickhouse/client": "catalog:",
    "@nestjs/common": "catalog:",
    "@nestjs/config": "catalog:",
    "@nestjs/core": "catalog:",
    "@nestjs/platform-express": "catalog:",
    "reflect-metadata": "catalog:",
    "rxjs": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@nestjs/testing": "catalog:",
    "@types/supertest": "catalog:",
    "pino-pretty": "^13.1.3",
    "supertest": "catalog:"
  }
}
