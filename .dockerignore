**/node_modules
**/dist
**/coverage
**/*.tsbuildinfo

# The migrate stage builds from source inside the image, so a host target/ is
# never wanted here — and it is the largest directory in the repository by an
# order of magnitude.
target

.git
.github
docs

**/.env
**/.env.*
!**/.env.example

**/*.log
**/.DS_Store
.vscode
.idea
